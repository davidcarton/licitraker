const router = require('express').Router()

router.post('/register', (req, res) => res.json({ msg: 'TODO' }))
router.post('/login', (req, res) => res.json({ msg: 'TODO' }))
router.post('/logout', (req, res) => res.json({ msg: 'TODO' }))
router.get('/me', (req, res) => res.json({ msg: 'TODO' }))

module.exports = router
